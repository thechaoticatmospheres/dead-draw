# Public server address only. VITE_ variables are embedded in the browser bundle.
# Never put API keys, tokens, passwords or private settings here.
VITE_GAME_SERVER_URL=
# Example production base for a project site; local development normally uses /.
BASE_PATH=/
